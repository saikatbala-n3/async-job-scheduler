# API Settings
PROJECT_NAME=Async Job Scheduler
DEBUG=False

# Database Settings (use localhost for migrations, db for Docker)
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=job_scheduler
POSTGRES_PORT=5432

# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=

JOB_QUEUE_NAME=jobs:queue
JOB_PROCESSING_NAME=jobs:processing
JOB_RESULT_TTL=3600
MAX_RETRIES=3
RETRY_DELAY=5

# Worker Settings
WORKER_CONCURRENCY=10
WORKER_POLL_INTERVAL=1

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000"]
